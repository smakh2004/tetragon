<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    tools:context=".questions.XpGainedActivity">

    <!-- LESSON FINISHED ANIMATION
         height: top of screen -> top of the continue button -->
    <app.rive.runtime.kotlin.RiveAnimationView
        android:id="@+id/xpAnimation"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/continue_btn_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:riveAlignment="CENTER"
        app:riveAutoPlay="true"
        app:riveFit="LAYOUT"
        app:riveResource="@raw/lesson_finished"
        app:riveStateMachine="State Machine 1" />

    <!-- CONTINUE BUTTON — revealed 2s after the screen opens.
         INVISIBLE (not GONE) so the animation keeps its size. -->
    <FrameLayout
        android:id="@+id/continue_btn_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp"
        android:alpha="0"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:paddingBottom="5dp"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <View
            android:id="@+id/continue_enabled_button_background"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/custom_button"
            android:backgroundTint="@color/black_2"
            android:translationY="5dp" />

        <Button
            android:id="@+id/continue_enabled_btn"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/custom_button"
            android:fontFamily="@font/inter_semibold"
            android:stateListAnimator="@drawable/button_animator"
            android:text="@string/continue_text"
            android:textColor="@color/white"
            android:textSize="17sp"
            app:backgroundTint="@color/black_3" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>